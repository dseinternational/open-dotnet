// <auto-generated/>

#nullable enable annotations
#nullable disable warnings

#pragma warning disable CA2225 // Operator overloads have named alternates


namespace DSE.Open.Observations;

[global::System.ComponentModel.TypeConverter(typeof(global::DSE.Open.Values.ValueTypeConverter<BehaviorFrequency, byte>))]
public readonly partial struct BehaviorFrequency
{
    private readonly byte _value;

    private BehaviorFrequency(byte value, bool skipValidation = false)
    {
        if (!skipValidation)
        {
            EnsureIsValidValue(value);
        }

        _value = value;
    }

    private static void EnsureIsValidValue(byte value)
    {
        if (!IsValidValue(value))
        {
            throw new global::System.ArgumentOutOfRangeException(nameof(value), value,
                $"'{value}' is not a valid {nameof(BehaviorFrequency)} value");
        }
    }

    public static bool TryFromValue(byte value, out BehaviorFrequency result)
    {
        if (IsValidValue(value))
        {
            result = new BehaviorFrequency(value, true);
            return true;
        }
    
        result = default;
        return false;
    }

    public static BehaviorFrequency FromValue(byte value)
    {
        EnsureIsValidValue(value);
        return new(value, true);
    }

    public static explicit operator BehaviorFrequency(byte value)
        => FromValue(value);

    static byte global::DSE.Open.IConvertibleTo<BehaviorFrequency, byte>.ConvertTo(BehaviorFrequency value)
        => (byte)value;

    public static implicit operator byte(BehaviorFrequency value)
    {
        return value._value;
    }

    // IEquatable<T>

    public bool Equals(BehaviorFrequency other) => _value.Equals(other._value);

    public override bool Equals(object? obj) => obj is BehaviorFrequency other && Equals(other);

    public override int GetHashCode()
    {
        return _value.GetHashCode();
    }

    // IEqualityOperators<BehaviorFrequency, BehaviorFrequency, bool>

    public static bool operator ==(BehaviorFrequency left, BehaviorFrequency right) => left.Equals(right);
    
    public static bool operator !=(BehaviorFrequency left, BehaviorFrequency right) => !(left == right);

    // ISpanFormattable

    public bool TryFormat(
        global::System.Span<char> destination,
        out int charsWritten,
        global::System.ReadOnlySpan<char> format,
        global::System.IFormatProvider? provider)
    {
        return ((global::System.ISpanFormattable)_value).TryFormat(destination, out charsWritten, format, provider);
    }

    public bool TryFormat(
        global::System.Span<char> destination,
        out int charsWritten)
        => TryFormat(destination, out charsWritten, default, default);

    public bool TryFormatInvariant(
        global::System.Span<char> destination,
        out int charsWritten,
        global::System.Span<char> format)
        => TryFormat(destination, out charsWritten, format, global::System.Globalization.CultureInfo.InvariantCulture);

    public bool TryFormatInvariant(
        global::System.Span<char> destination,
        out int charsWritten)
        => TryFormatInvariant(destination, out charsWritten, default);

    /// <summary>
    /// Gets a representation of the <see cref="BehaviorFrequency"/> value as a string with formatting options.
    /// </summary>
    [global::System.Runtime.CompilerServices.SkipLocalsInit]
    public string ToString(string? format, global::System.IFormatProvider? formatProvider)
    {
        return ((global::System.IFormattable)_value).ToString(format, formatProvider);
    }

    public string ToStringInvariant(string? format)
    {
        return ToString(format, global::System.Globalization.CultureInfo.InvariantCulture);
    }

    public string ToStringInvariant()
    {
        return ToStringInvariant(default);
    }

    /// <summary>
    /// Gets a representation of the BehaviorFrequency value as a string with default formatting options.
    /// </summary>
    /// <returns>
    /// A representation of the BehaviorFrequency value.
    /// </returns>
    public override string ToString()
    {
        return ToString(default, default);
    }

    // ISpanParsable<BehaviorFrequency>

    public static BehaviorFrequency Parse(global::System.ReadOnlySpan<char> s, global::System.IFormatProvider? provider)
        => global::DSE.Open.Values.ValueParser.Parse<BehaviorFrequency, byte>(s, provider);

    public static BehaviorFrequency ParseInvariant(global::System.ReadOnlySpan<char> s)
        => Parse(s, global::System.Globalization.CultureInfo.InvariantCulture);

    public static bool TryParse(
        global::System.ReadOnlySpan<char> s,
        global::System.IFormatProvider? provider,
        out BehaviorFrequency result)
        => global::DSE.Open.Values.ValueParser.TryParse<BehaviorFrequency, byte>(s, provider, out result);

    public static bool TryParse(
        global::System.ReadOnlySpan<char> s,
        out BehaviorFrequency result)
        => TryParse(s, default, out result);

    public static bool TryParseInvariant(
        global::System.ReadOnlySpan<char> s,
        out BehaviorFrequency result)
        => TryParse(s, global::System.Globalization.CultureInfo.InvariantCulture, out result);

    // IParsable<BehaviorFrequency>

    public static BehaviorFrequency Parse(string s, global::System.IFormatProvider? provider)
        => global::DSE.Open.Values.ValueParser.Parse<BehaviorFrequency, byte>(s, provider);

    public static BehaviorFrequency Parse(string s)
        => Parse(s, default);

    public static BehaviorFrequency ParseInvariant(string s)
        => Parse(s, global::System.Globalization.CultureInfo.InvariantCulture);

    public static bool TryParse(
        string? s,
        global::System.IFormatProvider? provider,
        out BehaviorFrequency result)
    {
        if (s is null)
        {
            result = default;
            return false;
        }
    
        return TryParse(global::System.MemoryExtensions.AsSpan(s), provider, out result);
    }

    public static bool TryParse(
        string? s,
        out BehaviorFrequency result)
        => TryParse(s, default, out result);

    public static bool TryParseInvariant(
        string? s,
        out BehaviorFrequency result)
        => TryParse(s, global::System.Globalization.CultureInfo.InvariantCulture, out result);

    // IUtf8SpanFormattable

    public bool TryFormat(
        global::System.Span<byte> utf8Destination,
        out int bytesWritten,
        global::System.ReadOnlySpan<char> format,
        global::System.IFormatProvider? provider)
        => ((global::System.IUtf8SpanFormattable)_value).TryFormat(utf8Destination, out bytesWritten, format, provider);

    // IUtf8SpanParsable<BehaviorFrequency>

    public static BehaviorFrequency Parse(
        global::System.ReadOnlySpan<byte> utf8Source,
        global::System.IFormatProvider? provider)
        => global::DSE.Open.Values.ValueParser.Parse<BehaviorFrequency, byte>(utf8Source, provider);

    public static bool TryParse(
        global::System.ReadOnlySpan<byte> utf8Source,
        global::System.IFormatProvider? provider,
        out BehaviorFrequency result)
        => global::DSE.Open.Values.ValueParser.TryParse<BehaviorFrequency, byte>(utf8Source, provider, out result);

    public int CompareTo(BehaviorFrequency other)
    {
        return _value.CompareTo(other._value);
    }

    // IComparisonOperators<BehaviorFrequency, BehaviorFrequency, bool>

    public static bool operator <(BehaviorFrequency left, BehaviorFrequency right) => left.CompareTo(right) < 0;
    
    public static bool operator >(BehaviorFrequency left, BehaviorFrequency right) => left.CompareTo(right) > 0;
    
    public static bool operator <=(BehaviorFrequency left, BehaviorFrequency right) => left.CompareTo(right) <= 0;
    
    public static bool operator >=(BehaviorFrequency left, BehaviorFrequency right) => left.CompareTo(right) >= 0;

}

