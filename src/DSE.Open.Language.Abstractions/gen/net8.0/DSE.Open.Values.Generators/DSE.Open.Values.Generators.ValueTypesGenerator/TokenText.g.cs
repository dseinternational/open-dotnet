// <auto-generated/>

#nullable enable annotations
#nullable disable warnings

#pragma warning disable CA2225

using System;
using System.ComponentModel;
using DSE.Open.Values;

namespace DSE.Open.Language;

[TypeConverter(typeof(global::DSE.Open.Values.ValueTypeConverter<TokenText, CharSequence>))]
public readonly partial struct TokenText
{

    private readonly CharSequence _value;
    private readonly bool _initialized;

    private TokenText(CharSequence value, bool skipValidation = false)
    {

        if (!skipValidation)
        {
            EnsureIsValidArgumentValue(value);
        }

        _value = value;
        _initialized = true;
    }

    public bool IsInitialized => _initialized;

    private static void EnsureIsValidArgumentValue(CharSequence value)
    {
        if (!IsValidValue(value))
        {
            throw new ArgumentOutOfRangeException(nameof(value), value,
                $"'{value}' is not a valid {nameof(TokenText)} value");
        }
    }

    private void EnsureInitialized()
    {
        UninitializedValueException<TokenText, CharSequence>.ThrowIfUninitialized(this);
    }

    public static bool TryFromValue(CharSequence value, out TokenText result)
    {
        if (IsValidValue(value))
        {
            result = new TokenText(value, true);
            return true;
        }
    
        result = default;
        return false;
    }

    public static TokenText FromValue(CharSequence value)
    {
        EnsureIsValidArgumentValue(value);
        return new(value, true);
    }

    public static explicit operator TokenText(CharSequence value)
        => FromValue(value);

    static CharSequence global::DSE.Open.IConvertibleTo<TokenText, CharSequence>.ConvertTo(TokenText value)
        => (CharSequence)value;

    public static explicit operator CharSequence(TokenText value)
    {
        value.EnsureInitialized();
        return value._value;
    }

    // IEquatable<T>

    public bool Equals(TokenText other) => _value.Equals(other._value);

    public override bool Equals(object? obj) => obj is TokenText other && Equals(other);

    public override int GetHashCode()
    {
        EnsureInitialized();
        return HashCode.Combine(_value);
    }

    public static bool operator ==(TokenText left, TokenText right) => left.Equals(right);
    
    public static bool operator !=(TokenText left, TokenText right) => !(left == right);

    // ISpanFormattable

    public bool TryFormat(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? provider)
        {
            EnsureInitialized();
            return ((ISpanFormattable)_value).TryFormat(destination, out charsWritten, format, provider);
        }

    public bool TryFormat(
        Span<char> destination,
        out int charsWritten)
        => TryFormat(destination, out charsWritten, default, default);

    public bool TryFormatInvariant(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format)
        => TryFormat(destination, out charsWritten, format, System.Globalization.CultureInfo.InvariantCulture);

    public bool TryFormatInvariant(
        Span<char> destination,
        out int charsWritten)
        => TryFormatInvariant(destination, out charsWritten, default);

    /// <summary>
    /// Gets a representation of the <see cref="TokenText"/> value as a string with formatting options.
    /// </summary>
    /// <returns>
    /// A representation of the <see cref="TokenText"/> value.
    /// </returns>
    public string ToString(string? format, IFormatProvider? formatProvider)
    {
        EnsureInitialized();
        return _value.ToString(format, formatProvider);
    }

    public string ToStringInvariant(string? format)
        => ToString(format, System.Globalization.CultureInfo.InvariantCulture);

    public string ToStringInvariant()
        => ToStringInvariant(default);

    /// <summary>
    /// Gets a representation of the TokenText value as a string with default formatting options.
    /// </summary>
    /// <returns>
    /// A representation of the TokenText value.
    /// </returns>
    public override string ToString() => ToString(default, default);

    // ISpanParsable<T>

    public static bool TryParse(
        ReadOnlySpan<char> s,
        IFormatProvider? provider,
        out TokenText result)
        => global::DSE.Open.Values.ValueParser.TryParse<TokenText, CharSequence>(s, provider, out result);

    public static bool TryParse(
        ReadOnlySpan<char> s,
        out TokenText result)
        => TryParse(s, default, out result);

    public static bool TryParseInvariant(
        ReadOnlySpan<char> s,
        out TokenText result)
        => TryParse(s, System.Globalization.CultureInfo.InvariantCulture, out result);

    public static bool TryParse(
        string? s,
        IFormatProvider? provider,
        out TokenText result)
        => global::DSE.Open.Values.ValueParser.TryParse<TokenText, CharSequence>(s, provider, out result);

    public static bool TryParse(
        string? s,
        out TokenText result)
        => TryParse(s, default, out result);

    public static bool TryParseInvariant(
        string? s,
        out TokenText result)
        => TryParse(s, System.Globalization.CultureInfo.InvariantCulture, out result);

    public static TokenText Parse(ReadOnlySpan<char> s, IFormatProvider? provider)
        => global::DSE.Open.Values.ValueParser.Parse<TokenText, CharSequence>(s, provider);

    public static TokenText Parse(ReadOnlySpan<char> s)
        => Parse(s, default);

    public static TokenText ParseInvariant(ReadOnlySpan<char> s)
        => Parse(s, System.Globalization.CultureInfo.InvariantCulture);

    public static TokenText Parse(string s, IFormatProvider? provider)
        => global::DSE.Open.Values.ValueParser.Parse<TokenText, CharSequence>(s, provider);

    public static TokenText Parse(string s)
        => Parse(s, default);

    public static TokenText ParseInvariant(string s)
        => Parse(s, System.Globalization.CultureInfo.InvariantCulture);

    public int CompareTo(TokenText other)
    {
        EnsureInitialized();
        return _value.CompareTo(other._value);
    }

    public static bool operator <(TokenText left, TokenText right) => left.CompareTo(right) < 0;
    
    public static bool operator >(TokenText left, TokenText right) => left.CompareTo(right) > 0;
    
    public static bool operator <=(TokenText left, TokenText right) => left.CompareTo(right) <= 0;
    
    public static bool operator >=(TokenText left, TokenText right) => left.CompareTo(right) >= 0;
}
