// <auto-generated/>

#nullable enable annotations
#nullable disable warnings

#pragma warning disable CA2225

using System;
using System.ComponentModel;
using DSE.Open.Values;

namespace DSE.Open.Records.Abstractions;

[TypeConverter(typeof(global::DSE.Open.Values.ValueTypeConverter<DiagnosisCode, ClinicalConceptCode>))]
public readonly partial struct DiagnosisCode
    : global::DSE.Open.Values.IEquatableValue<DiagnosisCode, ClinicalConceptCode>
{

    private readonly ClinicalConceptCode _value;
    private readonly bool _initialized;

    private DiagnosisCode(ClinicalConceptCode value, bool skipValidation = false)
    {

        if (!skipValidation)
        {
            EnsureIsValidArgumentValue(value);
        }

        _value = value;
        _initialized = true;
    }

    public bool IsInitialized => _initialized;

    private static void EnsureIsValidArgumentValue(ClinicalConceptCode value)
    {
        if (!IsValidValue(value))
        {
            throw new ArgumentOutOfRangeException(nameof(value), value,
                $"'{value}' is not a valid {nameof(DiagnosisCode)} value");
        }
    }

    private void EnsureInitialized()
    {
        UninitializedValueException<DiagnosisCode, ClinicalConceptCode>.ThrowIfUninitialized(this);
    }

    public static bool TryFromValue(ClinicalConceptCode value, out DiagnosisCode result)
    {
        if (IsValidValue(value))
        {
            result = new DiagnosisCode(value);
            return true;
        }
        
        result = default;
        return false;
    }

    public static DiagnosisCode FromValue(ClinicalConceptCode value)
    {
        EnsureIsValidArgumentValue(value);
        return new(value, true);
    }

    public static explicit operator DiagnosisCode(ClinicalConceptCode value)
        => FromValue(value);

    static ClinicalConceptCode global::DSE.Open.IConvertibleTo<DiagnosisCode, ClinicalConceptCode>.ConvertTo(DiagnosisCode value)
        => (ClinicalConceptCode)value;

    public static explicit operator ClinicalConceptCode(DiagnosisCode value)
    {
        value.EnsureInitialized();
        return value._value;
    }

    // IEquatable<T>

    public bool Equals(DiagnosisCode other) => _value.Equals(other._value);

    public override bool Equals(object? obj) => obj is DiagnosisCode other && Equals(other);

    public override int GetHashCode()
    {
        EnsureInitialized();
        return HashCode.Combine(_value);
    }

    public static bool operator ==(DiagnosisCode left, DiagnosisCode right) => left.Equals(right);
    
    public static bool operator !=(DiagnosisCode left, DiagnosisCode right) => !(left == right);

    // ISpanFormattable

    public bool TryFormat(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format,
        IFormatProvider? provider)
        {
            EnsureInitialized();
            return ((ISpanFormattable)_value).TryFormat(destination, out charsWritten, format, provider);
        }

    public bool TryFormat(
        Span<char> destination,
        out int charsWritten)
        => TryFormat(destination, out charsWritten, default, default);

    public bool TryFormatInvariant(
        Span<char> destination,
        out int charsWritten,
        ReadOnlySpan<char> format)
        => TryFormat(destination, out charsWritten, format, System.Globalization.CultureInfo.InvariantCulture);

    public bool TryFormatInvariant(
        Span<char> destination,
        out int charsWritten)
        => TryFormatInvariant(destination, out charsWritten, default);

    /// <summary>
    /// Gets a representation of the DiagnosisCode value as a string with formatting options.
    /// </summary>
    /// <returns>
    /// A representation of the DiagnosisCode value.
    /// </returns>
    public string ToString(string? format, IFormatProvider? formatProvider)
    {
        var maxCharLength = MaxSerializedCharLength;
    
        char[]? rented = null;
    
        try
        {
            Span<char> buffer = maxCharLength <= 128
                ? stackalloc char[maxCharLength]
                : (rented = System.Buffers.ArrayPool<char>.Shared.Rent(maxCharLength));
    
            _ = TryFormat(buffer, out var charsWritten, format, formatProvider);
    
            ReadOnlySpan<char> returnValue = buffer[..charsWritten];
            return new string(returnValue);
        }
        finally
        {
            if (rented is not null)
            {
                System.Buffers.ArrayPool<char>.Shared.Return(rented);
            }
        }
    
    }

    public string ToStringInvariant(string? format)
        => ToString(format, System.Globalization.CultureInfo.InvariantCulture);

    public string ToStringInvariant()
        => ToStringInvariant(default);

    /// <summary>
    /// Gets a representation of the DiagnosisCode value as a string with default formatting options.
    /// </summary>
    /// <returns>
    /// A representation of the DiagnosisCode value.
    /// </returns>
    public override string ToString() => ToString(default, default);

    // ISpanParsable<T>

    public static bool TryParse(
        ReadOnlySpan<char> s,
        IFormatProvider? provider,
        out DiagnosisCode result)
        => global::DSE.Open.Values.ValueParser.TryParse<DiagnosisCode, ClinicalConceptCode>(s, provider, out result);

    public static bool TryParse(
        ReadOnlySpan<char> s,
        out DiagnosisCode result)
        => TryParse(s, default, out result);

    public static bool TryParseInvariant(
        ReadOnlySpan<char> s,
        out DiagnosisCode result)
        => TryParse(s, System.Globalization.CultureInfo.InvariantCulture, out result);

    public static bool TryParse(
        string? s,
        IFormatProvider? provider,
        out DiagnosisCode result)
        => global::DSE.Open.Values.ValueParser.TryParse<DiagnosisCode, ClinicalConceptCode>(s, provider, out result);

    public static bool TryParse(
        string? s,
        out DiagnosisCode result)
        => TryParse(s, default, out result);

    public static bool TryParseInvariant(
        string? s,
        out DiagnosisCode result)
        => TryParse(s, System.Globalization.CultureInfo.InvariantCulture, out result);

    public static DiagnosisCode Parse(ReadOnlySpan<char> s, IFormatProvider? provider)
        => global::DSE.Open.Values.ValueParser.Parse<DiagnosisCode, ClinicalConceptCode>(s, provider);

    public static DiagnosisCode Parse(ReadOnlySpan<char> s)
        => Parse(s, default);

    public static DiagnosisCode Parse(string s, IFormatProvider? provider)
        => global::DSE.Open.Values.ValueParser.Parse<DiagnosisCode, ClinicalConceptCode>(s, provider);

    public static DiagnosisCode Parse(string s)
        => Parse(s, default);
}
